(() => {
  // Variables

  // Async/normal functions defined here

  chrome.runtime.onMessage.addListener((obj, sender, response) => {
    // Receives messages from the background and edits the content page
  });
})();

// Other Utility Functions
